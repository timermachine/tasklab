{
  "name": "tasklab",
  "version": "0.1.0",
  "description": "HITL task runner for service integrations — run by humans, guided by agents",
  "bin": {
    "tasklab": "./bin/tasklab.js"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e",
    "docs:run-report-assets": "node docs/scripts/generate-run-taskhub-report-assets.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "files": [
    "bin/",
    "lib/",
    "templates/"
  ],
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "tasklab",
    "hitl",
    "task-runner",
    "service-integration",
    "cli"
  ],
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/timermachine/tasklab.git"
  }
}
