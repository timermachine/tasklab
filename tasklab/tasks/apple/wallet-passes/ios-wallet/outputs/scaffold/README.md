# ios-wallet scaffold

This directory is copied into your target project root by:

`tasklab/tasks/apple/wallet-passes/ios-wallet/outputs/scripts/00-install-scaffold.sh`

Expected layout after install:

- `.env.example` → copy to `.env` (gitignored) and fill
- `pass/` → pass bundle source files
- `out/` → build outputs (gitignored)

