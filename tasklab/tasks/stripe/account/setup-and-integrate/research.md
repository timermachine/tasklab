# Research (verify online per run)

Update these files per run:

- `references/docs.md` (URLs you used)
- `references/checked-surfaces.yaml` (verified_on + tool versions)

Notes:

- Stripe Dashboard navigation labels can drift. Record the exact label/path you used in `outputs/reports/setup-report.md`.
- Keep secrets out of reports; record only key prefixes (e.g. `sk_test_...`, `pk_test_...`, `whsec_...`) and file paths.

